specVersion: "v1beta"
serviceId: "messagebox"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "messagebox-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
